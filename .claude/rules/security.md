\# 安全边界与防御规则

\- \*\*输入验证\*\*：所有来自外部的用户输入，必须在调用前进行严格的类型检查和长度限制。

\- \*\*凭证保护\*\*：严禁在代码中硬编码 API Key、密码或私钥。必须通过环境变量注入。

\- \*\*错误敏感信息屏蔽\*\*：在生产环境返回的错误信息中，不得包含堆栈追踪、数据库结构或敏感路径，需进行泛化处理。

\- \*\*输出过滤\*\*：处理 HTML 或 Markdown 内容时，必须进行 XSS 防范逃逸。

